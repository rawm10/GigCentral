
This test file covers the following scenarios:

1. `FormatToChordProAsync` method with input and `chordsOnly` flag set to `true`.
2. `FormatToChordProAsync` method with input and custom instructions.
3. `FormatForViewportAsync` method with `chordPro` and `ViewportInfo`.
4. `FormatForViewportAsync` method with `chordPro`, `ViewportInfo`, and `FormatOptions`.
5. `TransposeAsync` method with `chordPro`, `semitones`, and `useNashville`.
